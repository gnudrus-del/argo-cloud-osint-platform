__pycache__/
*.py[cod]
.venv/
.env
reports/
web_jobs/
.pytest_cache/

# Tool di terze parti vendorizzati + binari (521M, 27 repo git annidati) — ricreabili, fuori dal versionamento
tools/

# Screenshot/temporanei
tmp-*.png

# Deploy secrets — mai in git (i template/script/unit generici in
# deploy_artifacts/ SONO invece tracciati: sono referenziati da docs/DEPLOY.md)
.env
*.env
deploy_artifacts/secrets.env
deploy_artifacts/*.tar.gz

# Python build artifacts (rigenerati con `python -m build`)
dist/
build/
*.egg-info/
.eggs/
